---
description: Migrate one specified TypeScript/Tauri functional module into the correct Rust + Slint responsibilities.
agent: migration-orchestrator
---

Migrate this functional scope: $ARGUMENTS

Inventory first. Classify every TypeScript responsibility.

Use `tauri-decoupler`, `ts-rust-migrator`, `sshcli-specialist`, and `slint-ui-architect` as appropriate.

Preserve behavior and tests. Update the feature matrix with evidence.
