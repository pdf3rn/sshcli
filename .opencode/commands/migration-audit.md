---
description: Re-audit the existing Tauri/TypeScript implementation or a specified scope and refresh migration inventories.
agent: migration-orchestrator
---

Audit the existing source application for: $ARGUMENTS

If no scope is supplied, audit the whole migration-relevant application.

Use `source-auditor` and, where relevant, `tauri-decoupler` and `sshcli-specialist`.

Refresh the migration inventory and feature matrix. Do not implement unrelated changes.
