[package]
name = "sshcli-terminal-ui"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Minimal native Slint terminal surface for sshcli"
build = "build.rs"

[dependencies]
slint = "1.17.1"
sshcli-core = { path = "../core" }

[build-dependencies]
slint-build = "1.17.1"
